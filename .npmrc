# React 19 and react-scripts 5 declare conflicting peer ranges; this repo has
# always needed legacy peer resolution to install. Making it the default so a
# plain `npm ci` / `npm install` works the same locally, in Docker, and in CI —
# the desktop build in particular runs npm outside the container.
legacy-peer-deps=true
