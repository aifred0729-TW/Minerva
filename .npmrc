# React 19 + react-scripts 5 have conflicting peer ranges; this repo has always
# required legacy peer resolution to install. Making it the default so plain
# `npm ci` / `npm install` work locally, in Docker, and in CI.
legacy-peer-deps=true
